# SolidData MCP POC — copy to .env and fill in values.
# Required: SolidData management key and Gemini API key for the crew LLM.

SOLIDDATA_MANAGEMENT_KEY=your-soliddata-management-key-here
GEMINI_API_KEY=your-gemini-api-key-here
MODEL=gemini/gemini-2.0-flash

# Semantic layer ID for SolidData text2sql (required by MCP)
SEMANTIC_LAYER_ID=851b4156-e0ea-460b-b6f9-cf3f428e95b5

# For dev: use backend.dev.soliddata.io and mcp.dev.soliddata.io for AUTH_ENDPOINT and MCP_SERVER_URL
AUTH_ENDPOINT=https://backend.production.soliddata.io/api/v1/auth/exchange_user_access_key
MCP_SERVER_URL=https://mcp.production.soliddata.io/mcp

# Snowflake MCP (optional): when all four connection vars below are set, the crew executes
# the generated SQL in Snowflake and the Reporter analyzes query results; otherwise
# the Reporter only explains what the SQL does.
SNOWFLAKE_MCP_SERVER_URL=https://your-account.snowflakecomputing.com
SNOWFLAKE_DATABASE=
SNOWFLAKE_SCHEMA=
SNOWFLAKE_MCP_SERVER_NAME=
SNOWFLAKE_ACCESS_TOKEN=

# Optional: MCP tool name for SQL execution (default: sql_exec_tool)
SNOWFLAKE_SQL_TOOL_NAME=sql_exec_tool
